{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Clang++ - 调试",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${fileDirname}",
            "environment": [],
            "externalConsole": false, // 设为 true 会弹出终端窗口，false 则在下方调试控制台显示
            "MIMode": "lldb",         // Mac 必须是 lldb
            "preLaunchTask": "C/C++: clang++ 生成活动文件" // ⚠️这里必须和 tasks.json 里的 label 完全一致
        }
    ]
}